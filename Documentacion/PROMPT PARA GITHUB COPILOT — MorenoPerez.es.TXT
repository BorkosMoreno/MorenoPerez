# ?? PROMPT PARA GITHUB COPILOT — MorenoPerez.es

> **Cómo usar este prompt:** copia TODO el texto de este archivo (desde la sección "1. Rol" hasta el final) y pégalo en el chat de GitHub Copilot dentro de VS Code. Ejecútalo con `Ctrl + Enter`. Copilot generará el código en los archivos del proyecto.

---

## 1. Rol
Eres un desarrollador web **senior**, experto en **React + Vite**, HTML semántico, CSS accesible y despliegue en **GitHub Pages con dominio personalizado**. Trabajas de forma meticulosa: comentas el código en español, separas los datos de la lógica y evitas dependencias innecesarias.

## 2. Contexto del proyecto
- Soy un **particular** (uso personal, no profesional).
- Tengo una cuenta de GitHub (`BorkosMoreno@gmail.com`).
- Tengo el dominio **`MorenoPerez.es`** comprado en **IONOS** y configurado para apuntar a GitHub Pages.
- Actualmente el sitio es una sola página `index.html`.
- Quiero mantenerlo **gratuito, personal y seguro**.
- Uso **VS Code en español** con **GitHub Copilot (gratis)**.

## 3. Objetivo de esta tarea
Crear **únicamente** la **página principal (dashboard/portal)** y la **arquitectura base de despliegue**. Las demás páginas serán archivos HTML independientes con un aviso de **"en construcción"**. **No** implementes aún el contenido funcional de las páginas secundarias (gráficos, enlaces, notas, apuntes).

## 4. Arquitectura
- **Página principal (`/`)**: SPA con **React + Vite**. Es una sola pantalla (sin enrutado a otras URLs); la navegación entre secciones es por anclas dentro de la propia página.
- **Páginas secundarias**: archivos **HTML estáticos independientes** (`consumo.html`, `enlaces.html`, `notas.html`, `apuntes.html`), enlazados desde la página principal mediante enlaces normales.
- **Datos editables** en archivos **JSON separados**, bien comentados, **nunca** mezclados con el código.
- Todo alojado en **GitHub Pages**. (Las páginas que necesiten backend —notas por email, apuntes secretos— se podrán mover después a otra plataforma; ahora solo se crea el HTML de aviso).

## 5. Stack técnico
- **React + Vite** para la página principal.
- **CSS con variables** para el modo claro/oscuro (sin frameworks pesados). Si usas Tailwind, justifícalo y no añadas dependencias innecesarias.
- **HTML semántico** para las páginas secundarias.
- **Favicon SVG propio** (ligero, gratis).
- **Iconos**: SVG inline o una librería ligera gratuita (p. ej. `lucide-react` o SVG inline).

## 6. Página principal (`/`) — Requisitos

### 6.1 Diseño general
- Dashboard **minimalista**, mucho espacio en blanco, tipografía legible, colores suaves. Nada recargado.

### 6.2 Barra de navegación (Navbar)
- Siempre visible en la parte superior.
- En **móvil**: menú tipo **"hamburguesa"** que se despliega.
- Enlaces del navbar: las 4 secciones (anclas) + botón de modo claro/oscuro.

### 6.3 Cabecera (Header)
- Nombre del sitio (p. ej. "MorenoPerez.es").
- Un **lema breve**.

### 6.4 Cuatro tarjetas grandes (una por sección)
Cada tarjeta debe ser **completamente clicable** (toda la tarjeta es un enlace) y contener:
1. Un **icono** (gratis, ligero).
2. Un **título**.
3. Una **frase descriptiva**.

Las tarjetas son:
| # | Sección | Enlace | Icono sugerido |
|---|---------|--------|----------------|
| 1 | Monitorización de Consumo Eléctrico | `consumo.html` | ?? gráfico |
| 2 | Enlaces de Interés | `enlaces.html` | ?? enlaces |
| 3 | Notas | `notas.html` | ?? nota |
| 4 | Apuntes Secretos | `apuntes.html` | ?? candado |

> La tarjeta de **Apuntes Secretos** debe mostrar un **icono de candado** y **no revelar contenido**; solo indicar que **requiere clave**.

### 6.5 Responsive
- **Móvil**: tarjetas en **columna** (una debajo de otra).
- **Pantalla grande**: tarjetas en **cuadrícula** (2×2 o 4 en fila).

### 6.6 Modo claro/oscuro
- **Toggle** en el navbar.
- Persistir la preferencia en `localStorage`.
- Respeta `prefers-color-scheme` del sistema en el primer acceso.

### 6.7 Pie de página (Footer)
- Discreto, con el **año actual** (dinámico) y un **enlace de contacto**.

### 6.8 Accesibilidad
- HTML semántico: `<header>`, `<nav>`, `<main>`, `<section>`, `<footer>`.
- **Contraste correcto** en ambos modos (claro y oscuro).
- Navegable con **teclado**: focus visibles, orden de tabulación lógico.
- `aria-label` en iconos y controles sin texto.
- `lang="es"` en `<html>`.

## 7. Páginas secundarias (en construcción)
Crear `consumo.html`, `enlaces.html`, `notas.html`, `apuntes.html` dentro de la carpeta **`public/`** para que Vite los copie tal cual a `dist/`. Cada una debe tener:
- `<title>` propio y descriptivo.
- El mismo **favicon** que la página principal.
- Un aviso claro de **"Página en construcción"** con un enlace para volver al inicio (`index.html`).
- Respeto del **modo claro/oscuro** (reutiliza el mismo CSS/variables).
- La página `apuntes.html` debe mostrar un **candado** y un **campo de 4 dígitos** (solo la interfaz, sin funcionalidad real aún).
- Diseño consistente con la página principal (misma paleta de colores, tipografía).

## 8. Archivos de datos editables (estructura)
Crear la estructura (aunque estén vacíos o con datos de ejemplo) en **`public/datos/`**:
- `enlaces.json` — lista de enlaces organizados por categorías.
- `consumo.json` — datos de consumo eléctrico (fecha, kWh, precio, etc.).
- `notas.json` — notas.
- `apuntes.json` — apuntes (se cifrarán después).

Comentar cada archivo (en su `README` o en el propio JSON) explicando su estructura y cómo editarlo.

## 9. Requisitos técnicos transversales
- ? **Responsive** (móvil, tablet, PC).
- ? **Modo claro/oscuro**.
- ? **Rápida y ligera** (carga en segundos, sin dependencias pesadas).
- ? **Accesible** (contraste, teclado, semántica).
- ? **Favicon propio** y buen `<title>` en cada página.
- ? **Compatible con el dominio de IONOS**: respetar el archivo `CNAME` de GitHub Pages (contenido: `www.morenoperez.es`). **¡No borrarlo al publicar!**
- ? **HTTPS**: GitHub Pages lo ofrece gratis; indicar que se debe marcar **"Enforce HTTPS"**.
- ? **Fácil de mantener**: datos en archivos separados y comentados.

## 10. Configuración de despliegue (GitHub Pages)
- `vite.config.js`: usar `base: './'` para que los enlaces relativos funcionen tanto en local como en producción. (Si usas dominio raíz personalizado, también puedes usar `base: '/'`, pero `'./'` es más seguro).
- Archivo **`CNAME`** en `public/` con el contenido `www.morenoperez.es` (para que se copie a `dist/`).
- Workflow de **GitHub Actions** (`.github/workflows/deploy.yml`) para construir y desplegar en GitHub Pages automáticamente al hacer `push` a `main`.
- Los enlaces desde React a las páginas HTML deben ser **relativos** (`./consumo.html`, `./enlaces.html`, etc.) para funcionar en GitHub Pages.

## 11. Instrucciones de salida
- Crea **todos** los archivos necesarios.
- Comenta el código en **español**.
- No incluyas dependencias innecesarias.
- Verifica que `npm run build` funcione y que `dist/` contenga: `index.html`, los HTML secundarios, `CNAME`, la carpeta `datos/` y los assets.
- Al finalizar, resume brevemente lo creado y cómo probarlo en local (`npm run dev`) y desplegarlo (push a `main`).

---

**Fin del prompt.**
